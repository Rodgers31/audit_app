[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests

markers =
    network: test makes real outbound HTTP calls (opts out of the default network block)
